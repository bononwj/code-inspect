import { IOptions, IReporter, IStatistic } from 'jscpd';
export default class implements IReporter {
    private options;
    constructor(options: IOptions);
    report(...args: [any, IStatistic]): void;
    attach(): void;
    getStatus(statistic: IStatistic): string;
    getColor(statistic: IStatistic): string;
}
